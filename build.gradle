plugins {
    id 'base'
}

tasks.register('buildAllVersions') {
    group = 'build'
    description = 'Build every standalone CreateLazyTick version project.'
    dependsOn(
            ':common:build',
            ':forge-1.19.2:build',
            ':forge-1.20.1-0.5.1:build',
            ':forge-1.20.1-6.0.x:build',
            ':neoforge-1.21.1:build'
    )
}
